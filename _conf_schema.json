{
  "max_workers": {
    "type": "int",
    "description": "[下载配置] 最大并发线程数",
    "hint": "同时抓取多少个章节。调大可以提升正文下载速度；站点开始连续报 403/失败时，下载器也会提前停止继续派发新章节。",
    "default": 12
  },
  "search_max_workers": {
    "type": "int",
    "description": "[搜索配置] 最大并发线程数",
    "hint": "搜索书源时同时并发多少个请求。坏源很多时可以继续调大，例如 24 到 48；若频繁 429/403 则调小。",
    "default": 24
  },
  "request_timeout": {
    "type": "float",
    "description": "[下载配置] 单次请求超时（秒，必须大于 0）",
    "hint": "章节页面超过该时长未返回即视为失败，进入重试。填 0 或负数会在插件启动时直接报配置错误。",
    "default": 15.0
  },
  "search_request_timeout": {
    "type": "float",
    "description": "[搜索配置] 单个书源搜索请求超时（秒，必须大于 0）",
    "hint": "只影响搜书阶段。搜索通常可配得比正文下载更短，坏源多时可以调到 5 到 8 秒。",
    "default": 8.0
  },
  "search_time_budget": {
    "type": "float",
    "description": "[搜索配置] 单次跨书源搜索的总时间预算（秒，必须大于 0）",
    "hint": "到达这个时间预算后会提前返回当前已拿到的部分结果，避免 AstrBot 工具调用整体超时；搜索拿到足够多精确命中后也会提前收手。",
    "default": 60.0
  },
  "auto_probe_on_import": {
    "type": "bool",
    "description": "[健康探测] 导入后自动加入后台探测队列",
    "hint": "开启后，新导入书源会在后台异步做搜索/预检健康探测；不会阻塞插件启动、配置保存或函数工具返回。",
    "default": true
  },
  "probe_max_workers": {
    "type": "int",
    "description": "[健康探测] 后台探测线程数",
    "hint": "控制后台同时探测多少个书源。建议保持中等值，例如 4 到 8，避免与正文下载争抢资源。",
    "default": 6
  },
  "probe_keywords": {
    "type": "list",
    "description": "[健康探测] 探测搜索关键词列表",
    "hint": "后台探测时会依次使用这些关键词尝试搜书，用于判断书源是否可搜索并尽量拿到可预检样本书。",
    "items": {
      "type": "string"
    },
    "default": [
      "诡秘之主",
      "斗破苍穹",
      "凡人修仙传"
    ]
  },
  "use_env_proxy": {
    "type": "bool",
    "description": "[抓取配置] 沿用进程代理环境变量",
    "hint": "是否使用当前进程中的 http_proxy/https_proxy/no_proxy。默认关闭，避免被 AstrBot 全局代理设置影响书源抓取。",
    "default": false
  },
  "max_response_bytes": {
    "type": "int",
    "description": "[抓取配置] 单个 HTTP 响应解压后字节上限",
    "hint": "搜索、目录和正文页面超过该大小会立即终止读取，防止异常站点消耗过多内存；默认 8 MiB。",
    "default": 8388608
  },
  "max_retries": {
    "type": "int",
    "description": "[下载配置] 单章最大重试次数",
    "hint": "网络波动或目标站短暂异常时，单章最多重试多少次。",
    "default": 3
  },
  "retry_backoff": {
    "type": "float",
    "description": "[下载配置] 重试退避倍率",
    "hint": "失败重试的等待时间倍率，数值越大越保守。",
    "default": 1.6
  },
  "download_sample_chapters": {
    "type": "int",
    "description": "[质量配置] 下载前抽样章节数",
    "hint": "默认抽取开头、中间和末尾共 3 章，检查空壳、广告页和多个章节高度重复；调大将增加下载前请求数。",
    "default": 3
  },
  "download_sample_min_chars": {
    "type": "int",
    "description": "[质量配置] 抽样章节最少有效字符数",
    "hint": "移除 URL 和空白后低于该值的章节视为无效样本；默认 80。",
    "default": 80
  },
  "journal_fsync": {
    "type": "bool",
    "description": "[稳定性] 每次写入后强制落盘",
    "hint": "开启后更抗掉电，但会明显降低速度。普通 SSD 场景通常可关闭。",
    "default": false
  },
  "auto_assemble": {
    "type": "bool",
    "description": "[流程配置] 下载完成后自动组装 TXT",
    "hint": "开启后，后台任务抓完所有章节会自动生成最终 TXT。",
    "default": true
  },
  "notify_on_download_complete": {
    "type": "bool",
    "description": "[通知配置] 下载结束后通知请求者",
    "hint": "开启后，后台下载进入成功或失败终态时通知发起该轮下载的会话。回调保存失败不会影响下载任务启动。",
    "default": true
  },
  "notify_on_success": {
    "type": "bool",
    "description": "[通知配置] 通知下载成功结果",
    "hint": "仅控制 assembled/downloaded 终态；总通知开关关闭时此项不生效。",
    "default": true
  },
  "notify_on_failure": {
    "type": "bool",
    "description": "[通知配置] 通知下载失败结果",
    "hint": "仅控制 failed 终态；总通知开关关闭时此项不生效。",
    "default": true
  },
  "notify_mode": {
    "type": "string",
    "description": "[通知配置] 下载结束通知模式",
    "hint": "支持 llm、direct、off。默认 llm：下载终态主动唤醒模型生成通知文本，再由插件统一发送一次，避免模型工具发送与直接兜底重复；会产生额外模型调用成本。",
    "default": "llm"
  },
  "cleanup_journal_after_assemble": {
    "type": "bool",
    "description": "[流程配置] 组装完成后删除 JSONL 日志",
    "hint": "默认保留，便于审计和再次组装；开启后会删除 journal 节省空间。",
    "default": false
  },
  "default_encoding": {
    "type": "string",
    "description": "[抓取配置] 默认网页编码",
    "hint": "留空时自动猜测；常见值如 utf-8、gb18030、gbk。",
    "default": ""
  },
  "preview_chars": {
    "type": "int",
    "description": "[工具配置] 网页预览最大字符数",
    "hint": "函数工具 novel_fetch_preview 默认返回的字符数上限。",
    "default": 4000
  },
  "max_preview_fetch_chars": {
    "type": "int",
    "description": "[工具配置] 单次网页预览返回上限",
    "hint": "命令或工具调用 novel_fetch_preview 时允许返回的最大字符数上限。",
    "default": 1800
  },
  "max_tool_response_chars": {
    "type": "int",
    "description": "[工具配置] 单次工具摘要最大字符数",
    "hint": "控制函数工具写回给 LLM 的最大摘要体积，过大结果会更多落本地报告。",
    "default": 2800
  },
  "max_tool_preview_items": {
    "type": "int",
    "description": "[工具配置] 单次工具内联预览条数",
    "hint": "列表类工具默认最多在聊天窗口里内联多少条结果。",
    "default": 8
  },
  "max_tool_preview_text": {
    "type": "int",
    "description": "[工具配置] 单段文本预览最大长度",
    "hint": "搜索简介、错误摘要等单段文本在聊天窗口里的最大长度。",
    "default": 180
  },
  "book_sources": {
    "type": "list",
    "description": "[启动导入] 启动时自动导入的书源列表",
    "hint": "每项可填写 URL、文件路径，或原始 JSON 文本。插件启动后会在后台依次导入；成功导入过且配置未变化的项会跳过重复导入。",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "clean_rule_sources": {
    "type": "list",
    "description": "[启动导入] 启动时自动导入的净化规则列表",
    "hint": "每项可填写 URL、文件路径，或原始规则文本/JSON。插件启动后会在后台依次导入；成功导入过且配置未变化的项会跳过重复导入。",
    "items": {
      "type": "string"
    },
    "default": []
  },
  "auto_install_bundled_skills": {
    "type": "bool",
    "description": "[启动导入] 自动安装插件自带 skill",
    "hint": "默认开启。安装会在后台执行，并写入 bootstrap_state.json；失败只记录状态和日志，不影响插件主功能。关闭后仍会保留书源和净化规则的启动导入。",
    "default": true
  },
  "user_agent": {
    "type": "string",
    "description": "[抓取配置] HTTP User-Agent",
    "hint": "访问目标书站时携带的 User-Agent。",
    "default": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/125.0 Safari/537.36"
  }
}
